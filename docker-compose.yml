version: '3.9'
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend/files:/app/files

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend